const { default: CartIcon } = require("./CartIcon")

const cartIconButton = (props) => {
    return (
        <button>
            <CartIcon />
        </button>
    );
};


export default cartIconButton;