import React from 'react';
import './Cart.css';

const Cart = (props) => {
  return (
   <button className="cart">
   <span>Yourt Cart</span>
   <span>3</span>
   </button>
   );
};

export default Cart;